# ── 当前运行配置 ──
# 直接修改此文件，重启生效: docker compose up -d --build
# 完整参数说明见 .env.example

MODEL_SIZE=large-v3-turbo
DEVICE=cpu
COMPUTE_TYPE=int8
LANGUAGE=
MAX_CLIENTS=2
SESSION_TIMEOUT_SEC=600
SILENCE_TIMEOUT_SEC=10
LOG_LEVEL=INFO
